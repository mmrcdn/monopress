<svg width="512" height="512" viewBox="0 0 64 64" xmlns="http://www.w3.org/2000/svg">
  <!-- Monopress Favicon — Option B: Markdown Hash -->
  <!-- Background -->
  <rect width="64" height="64" rx="14" fill="#f5f5f3"/>
  <!-- Border frame -->
  <rect x="1" y="1" width="62" height="62" rx="13" fill="none" stroke="#1a1a1a" stroke-width="2"/>
  <!-- # hash mark -->
  <!-- Vertical bar left -->
  <rect x="21" y="14" width="4" height="36" rx="1" fill="#1a1a1a"/>
  <!-- Vertical bar right -->
  <rect x="39" y="14" width="4" height="36" rx="1" fill="#1a1a1a"/>
  <!-- Horizontal bar top -->
  <rect x="13" y="24" width="38" height="4" rx="1" fill="#1a1a1a"/>
  <!-- Horizontal bar bottom -->
  <rect x="13" y="36" width="38" height="4" rx="1" fill="#1a1a1a"/>
</svg>
